# Status of the Backend ['local', 'online']
STATUS='local'

# Backend url for local
BACKEND_URL_LOC = 'http://localhost:4000'
WS_URL_LOC = 'ws://localhost:4000'

# Backend url for cloud
BACKEND_URL_ONL = 'https://lib-backend-atg1.onrender.com/'
WS_URL_ONL = 'ws://lib-backend-atg1.onrender.com/'